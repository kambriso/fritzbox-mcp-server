variables:
  RUNNER_GENERATE_ARTIFACTS_METADATA: "true"

test:
  image: golang:1.25
  script:
    - make tidy
    - make test

release:
  image: golang:1.25
  only:
    - tags
  script:
    - make dists
  artifacts:
    paths:
      - build/
